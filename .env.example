# OpenRouter LLM Configuration (Required)
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
OPENROUTER_API_KEY=<insert your openrouter api key here>
OPENROUTER_MODEL=<ex: meta-llama/llama-3.3-70b-instruct:free>

# Model Recommendations (in order of quality for RAG tasks):
#
# BEST (Paid but Cheap - Recommended for Production):
# - anthropic/claude-3-haiku              ← Best faithfulness, excellent at following instructions
# - meta-llama/llama-3.1-70b-instruct     ← Strong reasoning, good grounding
# - mistralai/mistral-7b-instruct         ← Budget-friendly, decent quality
#
# GOOD (Free but Rate Limited - OK for Testing):
# - meta-llama/llama-3.3-70b-instruct:free  ← Best free option
# - meta-llama/llama-3.1-8b-instruct        ← Fast, moderate quality
#
# AVOID (Too Weak for RAG):
# - qwen/qwen3-4b:free                    ← Hallucinates frequently, poor instruction following
# - Small (<7B) models                    ← Generally unreliable for grounded generation
#
# Note: Free models often have poor faithfulness scores (~40-60%).
#       For production, use Claude Haiku (~90%+ faithfulness) or Llama 70B (~85%+)

# PubMed/Entrez Configuration (Required)
ENTREZ_EMAIL=your.email@example.com
ENTREZ_API_KEY=<api key for entrez, optional but recommended for higher rate limits>

# PubMed Query Configuration (Optional)
# Default query used when building index if none specified
PUBMED_DEFAULT_QUERY='GLP-1 cardiovascular outcomes'

# API Configuration (Optional)
# URL for the FastAPI backend (used by Streamlit frontend)
API_URL=http://localhost:8000
